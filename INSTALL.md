# 📦 Инструкция по установке и запуску

## 🚀 Быстрый старт

### 1. Установка библиотек

Откройте терминал (PowerShell или CMD) в папке проекта и выполните:

```bash
pip install -r requirements.txt
```

Или установите библиотеки по отдельности:

```bash
pip install aiogram==3.4.1
pip install python-dotenv==1.0.0
pip install aiohttp==3.9.1
```

### 2. Настройка токена

Токен уже добавлен в файл `env_example.txt`. 

**Для локального запуска:**
- Создайте файл `.env` в корне проекта
- Скопируйте туда содержимое из `env_example.txt`:

```
BOT_TOKEN=8243582284:AAGcbvrQ1B_5Rsudn2HjawQigFJWXNwOIE0
WEBHOOK_HOST=
PORT=8000
```

Или просто переименуйте `env_example.txt` в `.env`

### 3. Запуск бота локально

```bash
python bot.py
```

Бот запустится и будет работать в режиме polling (постоянно проверяет новые сообщения).

### 4. Проверка работы

1. Откройте Telegram
2. Найдите вашего бота по имени (которое вы указали при создании у @BotFather)
3. Отправьте команду `/start`
4. Проверьте работу всех кнопок

## 📋 Команды для копирования

### Windows (PowerShell):
```powershell
# Установка библиотек
pip install -r requirements.txt

# Создание .env файла (если его нет)
Copy-Item env_example.txt .env

# Запуск бота
python bot.py
```

### Windows (CMD):
```cmd
pip install -r requirements.txt
copy env_example.txt .env
python bot.py
```

### Linux/Mac:
```bash
pip install -r requirements.txt
cp env_example.txt .env
python bot.py
```

## ⚠️ Важные замечания

1. **Токен бота** - уже вставлен в `env_example.txt`. Не публикуйте его в открытом доступе!

2. **Python версия** - требуется Python 3.11 или выше. Проверить версию:
   ```bash
   python --version
   ```

3. **Если возникают ошибки**:
   - Убедитесь, что все библиотеки установлены
   - Проверьте, что файл `.env` существует и содержит токен
   - Проверьте версию Python

## 🌐 Деплой на Render

См. раздел "Деплой на Render" в файле `README.md`


