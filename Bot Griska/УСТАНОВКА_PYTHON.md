# 🐍 Установка Python и библиотек

## ❌ Проблема: `pip` не распознано

Это означает, что Python не установлен на вашем компьютере или не добавлен в PATH.

## ✅ Решение: Установка Python

### Шаг 1: Скачать Python

1. Перейдите на официальный сайт: https://www.python.org/downloads/
2. Нажмите кнопку **"Download Python 3.11.x"** (или последнюю версию)
3. Скачайте установщик для Windows

### Шаг 2: Установить Python

1. Запустите скачанный файл (например, `python-3.11.0-amd64.exe`)
2. **ВАЖНО!** Отметьте галочку **"Add Python to PATH"** внизу окна установщика
3. Нажмите **"Install Now"**
4. Дождитесь завершения установки
5. Нажмите **"Close"**

### Шаг 3: Проверка установки

Откройте **новый** PowerShell или CMD и выполните:

```powershell
python --version
```

Должно показать версию, например: `Python 3.11.0`

Также проверьте pip:

```powershell
pip --version
```

Должно показать версию pip, например: `pip 23.0.1`

## 📦 Установка библиотек для бота

После установки Python выполните:

```powershell
pip install -r requirements.txt
```

Или установите по отдельности:

```powershell
pip install aiogram==3.4.1
pip install python-dotenv==1.0.0
pip install aiohttp==3.9.1
```

## 🔄 Если Python уже установлен, но pip не работает

### Вариант 1: Использовать `python -m pip`

```powershell
python -m pip install -r requirements.txt
```

### Вариант 2: Использовать `py` команду (Windows)

```powershell
py -m pip install -r requirements.txt
```

### Вариант 3: Переустановить Python с галочкой "Add to PATH"

## ⚠️ Важные замечания

1. **После установки Python** обязательно закройте и откройте заново PowerShell/CMD
2. Убедитесь, что галочка **"Add Python to PATH"** была отмечена при установке
3. Если проблемы остаются, перезагрузите компьютер

## 🚀 После установки

Когда Python и pip будут работать, выполните:

```powershell
# Создать .env файл
Copy-Item env_example.txt .env

# Запустить бота
python bot.py
```

## 📞 Если ничего не помогает

1. Проверьте, установлен ли Python:
   - Откройте "Параметры Windows" → "Приложения"
   - Найдите "Python" в списке установленных программ

2. Если Python установлен, но команды не работают:
   - Переустановите Python с галочкой "Add to PATH"
   - Или добавьте Python вручную в переменную PATH

3. Альтернатива: Используйте Microsoft Store
   - Откройте Microsoft Store
   - Найдите "Python 3.11"
   - Установите оттуда (автоматически добавит в PATH)


